# Operator-local env vars loaded by the justfile; only .env.example is checked in.
.env

# Real grantee mapping (contains PII); only .example is checked in.
**/grantees.json

# Operator-local Helm overrides (may contain PII like phone numbers).
helm/philanthropy-os/values.local.yaml

# Build output.
dist/
