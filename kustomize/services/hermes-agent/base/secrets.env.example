# Copy this file to secrets.env and fill in real values.
# secrets.env is gitignored and consumed by the kustomize secretGenerator.

# Gateway API auth key. Minimum 8 chars; generate with: openssl rand -hex 32
API_SERVER_KEY=

# Model provider API keys (add whichever the agent uses).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
