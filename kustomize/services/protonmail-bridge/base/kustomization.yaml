apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: protonmail-bridge
      app.kubernetes.io/part-of: philanthropy-os

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: philanthropy-os/protonmail-bridge
    newTag: v3.19.0
